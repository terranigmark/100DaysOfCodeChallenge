let base = 5
let height = 7

const triangleArea = (base, height) => base * height / 2

console.log(`El área de un triángulo de base 
${base} y altura ${height} es: ${triangleArea(base, height)}`)
//console.log("El área de un triángulo de base 5 y altura 7 es: " + 5 * 7 /2) 

const pi = 3.1416
let radius = 8

const circleArea = (pi, radius) => pi * (radius * radius)
console.log(`The circle area with a radius of ${radius} is: ${circleArea(pi, radius)}`)